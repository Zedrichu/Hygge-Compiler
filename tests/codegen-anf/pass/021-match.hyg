type U = union {
  foo: int; 
  bar: string;
  baz: int
};

let x: U = baz{69};

let res: int =
  match x with {
      bar{y} -> 2;
      foo{y} -> y + 1;
      baz{y} -> y * 2
  };
assert(res = 138)
